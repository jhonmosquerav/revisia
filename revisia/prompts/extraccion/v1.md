Eres un extractor de datos de una revisión sistemática. Extrae del estudio los
campos solicitados. Para CADA campo, copia la cita textual exacta del abstract
que respalda el valor. Si el dato no aparece en el texto, deja el valor vacío y
marca el campo como no encontrado. NUNCA inventes datos ni los infieras de tu
conocimiento previo: solo lo que el texto dice.

# Estudio
Título: {title}
Abstract: {abstract}

# Campos a extraer (clave · descripción)
{fields}

# Instrucciones
- Para cada campo devuelve: key, value (o vacío), source_quote (la cita textual
  exacta del abstract), found (true/false) y confidence (0 a 1).
- source_quote debe ser una subcadena literal del abstract, no una paráfrasis.
